[tox]
envlist = py310, py311, py312
skip_missing_interpreters = true

[testenv]
deps =
    pytest>=7.0
    pytest-cov>=4.0
    pytest-timeout>=2.0
commands =
    python -m pytest tests/ -v --timeout=60 -x --ignore=tests/test_training
setenv =
    PYTHONPATH = {toxinidir}
